#csv파일로 만드는 것도 문제로 나옴 csv는 엑셀파일을 표형태로 만든 것
#다음 데이터를 csv로 작성한다음 이파일을 불러서 summary결과값을 작성해보시오.
#1,2,3,4,5는 var1 6,7,8,9,10은 var2로 저장해서 변수를 만들어 출력해라.
#결측치에 대한 문제 나옴
#데이터를 가져와서 분석해라
#복지패널로 가서 데이터를 가지고와서 노년층이 어디 지역이 가장많은가.
#인천광역시에서 어느 구가 가장 부유한가 등등 
#월급에 따른 연령대 -> 연령대에 따른 월급를 구하는 것 둘다 가능해야함.
#실전 데이터 분석 -> 8개중에 1개는 그대로 나오고 1개는 문제를 변형해서 만든다.
#깃허브에 저장을해서 시험때 깃허브에서 다운받아서 쓴다.